# Required: your OpenRouter API key
OPENROUTER_API_KEY=sk-or-...

# Flask session secret — change this in production
FLASK_SECRET_KEY=change-me-in-production

# Optional: persistent data directory (defaults to ./data)
DATA_DIR=data
